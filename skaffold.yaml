apiVersion: skaffold/v1alpha2
kind: Config

build:
  tagPolicy:
    sha256: {}
  artifacts:
    - imageName: sunfmin/skaffoldhello
      workspace: .
      docker:
        dockerfilePath: Dockerfile

deploy:
  kubectl:
    manifests:
      - ./k8s/*.yaml
